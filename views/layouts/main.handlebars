<!DOCTYPE html>
<html>

<head>
    <meta charset="UTF-8">
    {{#if title}}
    <title>{{title}}</title>
    {{else}}
    <title>Default title.</title>
    {{/if}}
    <link href="./css/article.css" rel="stylesheet" >
    <link href="https://cdn.quilljs.com/1.3.7/quill.snow.css" rel="stylesheet"> 
    <script src="https://cdn.quilljs.com/1.3.7/quill.js"></script>
    
</head>

<body>
    <div class="container">
        
        <div class="nav">
            <a href="./?sortby=desc"><img src="./images/home.png"></a>
            <a href="./yourArticles?sortby=desc"><img src="./images/blog.png"></a>
            <a href="./yourFavorites?sortby=desc"><img src="./images/favorites.png"></a>
            <a href="#"><img src="./images/message.png"></a>
            <a href="./login"><img src="./images/login.png"></a>
        </div>

        {{#if toastMessage}}
        <p>{{toastMessage}}</p>
        {{/if}}
        {{{body}}}
    </div>
</body>

</html>